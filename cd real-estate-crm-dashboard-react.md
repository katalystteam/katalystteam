cd real-estate-crm-dashboard-react
npm install          # if you haven’t already
npm run sync:clickup


git status                    # see what changed
git add src/data/generated/   # or add specific paths
git commit -m "Refresh listings from ClickUp"
git push