# This folder is the full Katalyst workspace (scripts, exports, site HTML, etc.).
# GitHub only tracks the CRM dashboard, the Iowa property scraper, and its
# CI workflow â€” keep everything else local.

/*
!/.gitignore
!/README.md
!/real-estate-crm-dashboard-react/
!/PropertyScraper/
!/.github/
!/.github/workflows/
!/ghl-alerts/
