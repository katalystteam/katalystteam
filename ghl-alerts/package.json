{
  "name": "katalyst-ghl-alerts",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test",
    "poll": "node --env-file-if-exists=.env poll.mjs"
  }
}
